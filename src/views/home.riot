<home>
  <a href="{ `/#!/user/${this.userId}/random` }">Click Me!</a>
  <script>
    export default {
      onBeforeMount() {
        this.userId = Math.round(Math.random() * 99999)
      }
    }
  </script>
</home>
